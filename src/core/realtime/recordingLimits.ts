/** Both final assembly and speaker transcription must accept the same recording size. */
export const MAX_RECORDING_BYTES = 64 * 1024 * 1024;
export const RECORDING_WARNING_BYTES = 48 * 1024 * 1024;
